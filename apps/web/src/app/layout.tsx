import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'GhostTip - Anonymous Tip Platform',
  description: 'Privacy-focused payment link aggregator for content creators',
  keywords: ['tips', 'donations', 'privacy', 'crypto', 'payments'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
