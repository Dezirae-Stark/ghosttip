<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="monero-glow">
      <feGaussianBlur stdDeviation="1" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Monero circle -->
  <circle cx="12" cy="12" r="9" stroke="#00FFFF" stroke-width="2" fill="none" filter="url(#monero-glow)"/>

  <!-- Monero M symbol (simplified) -->
  <path d="M7 8 L7 16 M7 8 L12 13 L17 8 L17 16"
        stroke="#00FFFF"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
        fill="none"
        filter="url(#monero-glow)"/>

  <!-- Bottom connectors -->
  <line x1="7" y1="16" x2="9" y2="16" stroke="#00FFFF" stroke-width="2" stroke-linecap="round" filter="url(#monero-glow)"/>
  <line x1="17" y1="16" x2="15" y2="16" stroke="#00FFFF" stroke-width="2" stroke-linecap="round" filter="url(#monero-glow)"/>
</svg>
