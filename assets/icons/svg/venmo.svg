<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="venmo-glow">
      <feGaussianBlur stdDeviation="1" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Rounded square container -->
  <rect x="3" y="3" width="18" height="18" rx="4" stroke="#00FFFF" stroke-width="2" fill="none" filter="url(#venmo-glow)"/>

  <!-- V lettermark -->
  <path d="M8 8 L12 16 L16 8" stroke="#00FFFF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" fill="none" filter="url(#venmo-glow)"/>
</svg>
